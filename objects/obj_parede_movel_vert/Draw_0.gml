/// @description Desenha variáveis de debug na tela
draw_self(); // Desenha a sprite da plataforma

// Desenha as variáveis de debug na tela
//draw_text(10, 10, "move_dir: " + string(move_dir));
//draw_text(10, 30, "vspeed: " + string(vspeed));
//draw_text(10, 50, "y: " + string(y));
