/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor

// Resetar o estado dos botões após a confirmação
if (global.selectedAnswer == "") {
    with (obj_btnesq) { isPressed = false; }
    with (obj_btndir) { isPressed = false; }
    with (obj_btnconf) { isPressed = false; }
}










